# Linting / tsc

Set up or fix ESLint and ensure `tsc --noEmit` passes cleanly.

Work in progress in a Cursor chat — check there for current state before picking up.

## Tasks

- [ ] `tsc --noEmit` clean (zero errors)
- [ ] ESLint configured and passing (`pnpm lint`)
- [ ] Add lint + tsc to CI / pre-commit or `pnpm test`
